"""压缩提示词：上下文压缩专用，独立成文件便于单独迭代调优。

与顶层 prompts.py 分工：那边是 Agent 的系统提示词（模型是谁、行为规则），
这里定义"压缩时如何概括历史"。REQUIRED_HEADINGS 与模板中的标题联动——
改标题务必同步，validate_summary 靠它识别模型是否按模板输出。
"""

SUMMARY_PROMPT = """你是上下文压缩器。把上面的对话历史压缩为结构化交接摘要，仅供后续继续任务使用。
禁止调用任何工具，只输出以下 Markdown（六个标题必须逐字保留）：

## 目标
<用户要做什么，逐字引用关键需求>

## 关键决策
<已确定的方案、约束与用户偏好>

## 已完成与进行中
<步骤、改动过的文件与内容要点>

## 阻碍
<失败的尝试与原因，保留以避免重蹈覆辙>

## 下一步
<必须引用对话原文的直接引语>

## 相关文件
<涉及路径及当前状态>"""

# 摘要必须包含的标题，缺失说明模型没按模板输出
REQUIRED_HEADINGS = ("## 目标", "## 下一步")
